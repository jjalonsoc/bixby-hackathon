action (PlayBook) {
  description (choose audiobook from library)
  type (Calculation)
  collect {
    input (audiobook) {
      type (Audiobook)
      min (Required) max (One)
    }
  }
  output (audioPlayer.AudioInfo)
}